[package]
name = "Bartend"
version = "0.1.0"
edition = "2024"

[dependencies]
iced = { version = "0.14.0", features = ["tokio", "wgpu", "debug"] }
rusqlite = { version = "0.38.0", features = ["bundled"] }
sql_query_builder = { version = "2.5.2", features = ["sqlite"] }

[dev-dependencies]
tempfile = "3.24.0"
